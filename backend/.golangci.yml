version: "2"

linters:
  settings:
    errcheck:
      exclude-functions:
        # Deferred Close() errors can't be acted on at the call site.
        - (io.Closer).Close
        - (*net/http.Response.Body).Close
        # fmt.Fprintf errors in test HTTP handlers are not actionable.
        - fmt.Fprintf
